/*
objecté um tipo que representa o tipo não primitivo, isto é, qualquer coisa que não é number, 
string, boolean, bigint, symbol, null, ou undefined.
*/

const dados: object = {
  nome: 'Alex',
  idade: '16',
  estado: 'BA',
}

console.log(dados);

