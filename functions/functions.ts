/*
Para começar, assim como no JavaScript, as funções TypeScript podem ser 
criadas como uma função nomeada ou como uma função anônima. Isso permite 
que você escolha a abordagem mais apropriada para seu aplicativo, se você 
está construindo uma lista de funções em uma API ou uma função única para 
passar para outra função.
*/

function printName(name) {
  console.log(name);
}

function sum(x, y) {
  return x + y;
}

